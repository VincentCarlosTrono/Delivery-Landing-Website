export interface ServiceDataInterface {
    title: string;
    image: any;
    description: string;
    more: string;
  }

  export interface ButtonPropsInterface {
    name: String;
    icon?: any;
  }
